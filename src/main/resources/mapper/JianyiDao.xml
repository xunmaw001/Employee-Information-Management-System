<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.JianyiDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        a.id as id
        ,a.yonghu_id as yonghuId
        ,a.jianyi_name as jianyiName
        ,a.jianyi_types as jianyiTypes
        ,a.insert_time as insertTime
        ,a.jianyi_content as jianyiContent
        ,a.create_time as createTime
    </sql>
    <select id="selectListView" parameterType="map" resultType="com.entity.view.JianyiView" >
        SELECT
        <include refid="Base_Column_List" />

--         级联表的字段
        ,yonghu.yonghu_name as yonghuName
        ,yonghu.yonghu_phone as yonghuPhone
        ,yonghu.yonghu_id_number as yonghuIdNumber
        ,yonghu.yonghu_photo as yonghuPhoto
        ,yonghu.sex_types as sexTypes
        ,yonghu.bumen_types as bumenTypes
        ,yonghu.zhiwei_types as zhiweiTypes

        FROM jianyi  a
        left JOIN yonghu yonghu ON a.yonghu_id = yonghu.id

        <where>
            <if test="params.yonghuId != null">
                and a.yonghu_id = #{params.yonghuId}
            </if>
            <if test=" params.jianyiName != '' and params.jianyiName != null and params.jianyiName != 'null' ">
                and a.jianyi_name like CONCAT('%',#{params.jianyiName},'%')
            </if>
            <if test="params.jianyiTypes != null">
                and a.jianyi_types = #{params.jianyiTypes}
            </if>
            <if test=" params.insertTimeStart != '' and params.insertTimeStart != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) >= UNIX_TIMESTAMP(#{params.insertTimeStart}) ]]>
            </if>
            <if test=" params.insertTimeEnd != '' and params.insertTimeEnd != null ">
                <![CDATA[  and UNIX_TIMESTAMP(a.insert_time) <= UNIX_TIMESTAMP(#{params.insertTimeEnd}) ]]>
            </if>
            <if test=" params.jianyiContent != '' and params.jianyiContent != null and params.jianyiContent != 'null' ">
                and a.jianyi_content like CONCAT('%',#{params.jianyiContent},'%')
            </if>

            <if test=" params.yonghuName != '' and params.yonghuName != null and params.yonghuName != 'null' ">
                and yonghu.yonghu_name like CONCAT('%',#{params.yonghuName},'%')
            </if>
            <if test=" params.yonghuPhone != '' and params.yonghuPhone != null and params.yonghuPhone != 'null' ">
                and yonghu.yonghu_phone like CONCAT('%',#{params.yonghuPhone},'%')
            </if>
            <if test=" params.yonghuIdNumber != '' and params.yonghuIdNumber != null and params.yonghuIdNumber != 'null' ">
                and yonghu.yonghu_id_number like CONCAT('%',#{params.yonghuIdNumber},'%')
            </if>
            <if test="params.sexTypes != null">
                and yonghu.sex_types = #{params.sexTypes}
            </if>

            <if test="params.bumenTypes != null">
                and yonghu.bumen_types = #{params.bumenTypes}
            </if>

            <if test="params.zhiweiTypes != null">
                and yonghu.zhiwei_types = #{params.zhiweiTypes}
            </if>

        </where>
        order by a.${params.orderBy} desc
    </select>

</mapper>